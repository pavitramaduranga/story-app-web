/**
 * Example Firebase configuration file.
 *
 * 1. Copy this file to firebase-config.js in the same folder.
 * 2. Replace each value with the settings from your Firebase project.
 * 3. Keep firebase-config.js out of version control if you prefer by adding it to .gitignore.
 */
window.firebaseConfig = {
  apiKey: 'YOUR_API_KEY',
  authDomain: 'your-project.firebaseapp.com',
  projectId: 'your-project-id',
  storageBucket: 'your-project.appspot.com',
  messagingSenderId: 'YOUR_SENDER_ID',
  appId: 'YOUR_APP_ID',
  // measurementId: 'G-XXXXXXX', // Optional
};
